Al hacer click en la zona de codigo, el cursor a veces se queda invertido hasta que desplazo el cursor con una flecha o cambio de modo.

La multiple seleccion no hace nada en el worktree. Se seleccionan, si, pero no permite mover varios archivos o eliminar varios archivos simultaneamente, por que estando seleccionado, al hacer un nuevo clic se pierde la seleccion.




En Banner.txt hay un prototipo de lo que quiero que aparezca en la misma pantalla de close Mini, sin importar si es modo mouse o no.
La idea es que lo implementes a modo pantalla de presentacion, y lo modifiques si es necesario para dar un aspecto mas profesional.
En la misma pantalla tiene que aparecer Open a file to start debajo del baner, y debajo de todo el boton de cerrar


